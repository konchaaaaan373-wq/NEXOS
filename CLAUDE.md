# CLAUDE.md

## ユーザー情報
- 内科系の医師・研究者・起業家
- 人材紹介会社「Neco」を運営している
- プログラミングは初心者（コードの説明は要点を簡潔に）
- PythonとJavaScript/TypeScriptの両方を使う

## 言語・文体
- 返答は必ず日本語で行う
- ですます調で統一する

## コーディングルール
- コードにはコメントを日本語で付ける（初心者が理解しやすいよう、要点のみ簡潔に）
- エラーが発生した場合は、原因と解決策を分かりやすく説明する
- 新しい概念やライブラリを使う際は、何のために使うのか一言添える
- 指示されたらすぐに実行してよい（事前確認は不要）

## 情報セキュリティ
- 患者の個人情報や診療情報を扱う際は、取り扱いに注意するようリマインドする

## プロジェクト概要
- このフォルダではWebアプリ開発とデータ分析を行っている
- necoは人材紹介会社Necoの関連プロジェクト
- 複数のサブプロジェクト（Mitas, necoなど）が存在する

## タスク実行スタイル（最重要ルール）
- **コードを書いたら必ず自分でテスト・検証する。「書いて終わり」は禁止。**
- すぐに完了することを求められていない場合は、時間をかけて自律的にプロジェクトの完成度を高める
- 必要に応じてWeb検索を行い、最新の他社比較や業界動向を調査する
- 途中で「ある程度で十分」と判断したり、完成度を過大評価しない
- 何度も反芻・検証・改善のフィードバックを繰り返し、ロングランでタスクを実行する
- **「完成度を高めて」「よく考えて」「しっかり作って」などの指示は、ハーネスエンジニアリングのフルサイクルを回す合図とみなす**

## デザインルール
- AI的なフォントや構成を避け、人間らしい自然なデザインを心がける
- Canvaなどの拡張機能を活用し、状況に応じてイメージを生成しながらデザインする
- デザインに絵文字を使わない（厳守）

## ワークフロールール（全プロジェクト共通）
1. `claude/` ブランチで開発する
2. 変更内容を明確に記述してコミットする
3. originへプッシュする
4. `gh pr create` でPR作成し、URLをユーザーに返す
5. PRリンクを必ず提示して終了する（ユーザーの操作は"Merge"のみ）

## ハーネスエンジニアリング運用ルール

### 概要
Orchestratorが統括し、Planner / Generator / Evaluator の役割を自動的に切り替えながら開発を進めるシステム。
ユーザーは要件を伝えるだけで、計画→実装→テストのサイクルが自動的に回る。

### エージェント構成
- **Orchestrator** (`.claude/agents/orchestrator.md`): 全体を統括し、自動的にサイクルを回す
- **Planner** (`.claude/agents/planner.md`): 要件を分析し、スプリント計画書を作成する
- **Generator** (`.claude/agents/generator.md`): 計画書に基づきコードを1機能ずつ実装する
- **Evaluator** (`.claude/agents/evaluator.md`): 実装をテスト・レビューし、フィードバックを返す

### 自動起動の条件（これが最も重要）
**ユーザーが明示的に指示しなくても、以下の条件に該当すればClaudeが自律的にハーネスエンジニアリングを起動する:**

#### 必ず起動する場合
- 新機能の追加（「作って」「実装して」「開発して」など）
- 複数ステップにわたる実装が必要な場合
- 明確なゴールがあり、計画→実装→テストのサイクルが有効な場合
- 「完成度を高めて」「よく考えて」「しっかり作って」「ちゃんと動くようにして」などの品質要求
- 「点検して」「チェックして」「検証して」「デバッグして」などの品質確認依頼

#### Claudeの判断で起動する場合
- 実装したコードの品質に自信がないとき → 自分でEvaluatorループを回す
- ビルドエラーやテスト失敗が発生したとき → 修正→再検証ループを自動実行
- 複数ファイルにまたがる変更が必要なとき → 計画を立ててから実装
- 「これで大丈夫かな？」と思ったとき → 迷わずテスト・検証ループを回す

#### 起動しない場合（通常対応）
- 単純な質問や説明の依頼
- 1ファイルの小さな修正（ただし修正後のビルド確認は必ず行う）
- 設定変更のみ

### 自動検証ルール（全タスク共通）
**以下のルールはハーネスエンジニアリングの起動有無に関わらず、常に適用する:**
1. コードを書いたら → `npm run build` で型エラーがないか確認する
2. APIを修正したら → curlまたはPlaywrightで実際にリクエストを送って動作確認する
3. UIを修正したら → devサーバーを起動してブラウザテストする
4. エラーが出たら → 原因を特定して修正し、再テストして合格するまでループを回す
5. 「たぶん大丈夫」で終わらせない。実際に動かして確認するまでが1タスク

### 開発サイクル
```
ユーザーの要件
    |
    v
[Phase 1: 計画] 要件分析 -> スプリント計画書を作成 -> docs/sprint/に保存
    |
    v
[Phase 2: 実装ループ] タスクごとに繰り返す
    |   実装（Generator）-> 評価（Evaluator）
    |       |
    |       +-- 合格 -> コミット -> 次のタスクへ
    |       +-- 不合格 -> 修正 -> 再評価（最大3回）
    |
    v
[Phase 3: 完了] 全タスク合格 -> プッシュ -> PR作成 -> リンク提示
```

### 運用ルール
1. **1タスク1サイクル**: 1タスクずつ実装し、必ず評価を受ける
2. **計画優先**: 計画書にないタスクを勝手に追加しない
3. **フィードバック必須**: 不合格の場合は必ず修正する
4. **リトライ上限**: 同一タスクの修正は最大3回まで。超過時はユーザーに判断を仰ぐ
5. **スプリント記録**: 計画書と評価結果は`docs/sprint/`に保存する
6. **Playwright活用**: Webアプリの場合、Playwright MCPでブラウザ動作を確認する
7. **進捗報告**: 各フェーズの開始・完了時にユーザーへ簡潔に報告する

### 実行方法
自律的なループを回す場合は以下のフラグを推奨:
```bash
claude --dangerously-skip-permissions
```

オーケストレーターを直接指定して実行する場合:
```bash
claude --agent orchestrator "ログイン機能を作って"
```

### Playwright MCP設定（設定済み）
`.claude/settings.json` に以下が設定済み:
```json
{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@anthropic-ai/mcp-server-playwright"]
    }
  }
}
```
